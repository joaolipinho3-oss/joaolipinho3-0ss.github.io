const buttonPlus = document.getElementById('soma') // Botão de +
const buttonMinus = document.getElementById('subtracao') // Botão de menos

const tela = document.getElementById('display-counter') // Tela do contador

const increment = document.getElementById('changeBy') // Mudar incremento

buttonMinus.addEventListener('click')